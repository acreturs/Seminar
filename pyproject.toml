[project]
name = "flowr-seminar"
version = "0.1.0"
description = "Colab-first workflow to reproduce FlowR paper comparisons on custom images."
requires-python = ">=3.10"
readme = "README.md"
dependencies = [
  "pyyaml>=6.0",
  "pyngrok>=7.0",
  "gdown>=5.0",
]

[tool.setuptools]
py-modules = []
